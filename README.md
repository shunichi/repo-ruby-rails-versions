# GitHubリポジトリの ruby version と rails version を調べるスクリプト

## Usage
```
$ bundle install
$ GITHUB_TOKEN=xxxxxxx bundle exec ruby scan.rb ORGANIZATION_NAME > all.json
$ ruby json_to_csv all.json > all.csv
```
